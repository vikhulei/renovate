import { Wrapper } from "./NavbarStyle"
import UpperBar from "./upperBar/UpperBar"

const Navbar = () => {
    return (
        <Wrapper>
            <UpperBar />
        </Wrapper>
    )
}

export default Navbar