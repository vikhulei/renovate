import {BackgroundLowerBar, WrapperLowerBar, LeftSection, ContactTab, RightSection, IconsTab} from "./LowerBarStyle"

const LowerBar = () => {
    return (
        <BackgroundLowerBar>
            
        </BackgroundLowerBar>
    )
}

export default LowerBar